/**
 * API Routes Registry
 * 
 * This file exports all API route handlers that will be mounted
 * in the main Express app.
 * 
 * Example routes:
 * - /api/properties
 * - /api/contacts
 * - /api/skiptraces
 * - /api/webhooks
 * - /api/agents
 */

import { Router } from 'express';

export const apiRouter = Router();

// TODO: Mount route handlers here
// Example:
// apiRouter.use('/properties', propertiesRouter);
// apiRouter.use('/contacts', contactsRouter);
// apiRouter.use('/skiptraces', skiptracesRouter);

export default apiRouter;
